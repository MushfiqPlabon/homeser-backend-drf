{
  "buildCommand": "python3 -m pip install uv && uv sync --locked && uv run manage.py collectstatic --noinput && uv run manage.py migrate",
  "builds": [
    {
      "src": "homeser/wsgi.py",
      "use": "@vercel/python",
      "config": { "python": "3.11.x" }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "homeser/wsgi.py"
    }
  ]
}
